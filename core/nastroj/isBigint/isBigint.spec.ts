import { describe, it, expect } from 'vitest';

import { args, falsey, stubFalse } from '../helper/test';

import { isBigint } from '.';

describe('(na) isBigint', () => {
  it('should return `true` for bigint', () => {});
});
